# HoloNight Images

Own synchronous image inspection, bounded raster decoding and structured metadata.
Use caller-owned seekable devices; never open a pathname. Keep caches, workers, UI, SVG rendering and animation in consumers.
Run provider CTest and the installed-package test before handoff. Preserve GPL-3.0-or-later provenance from Viewer.
